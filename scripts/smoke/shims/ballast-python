#!/usr/bin/env bash
set -euo pipefail
if [ ! -x /usr/local/lib/ballast/bin/ballast-python ]; then
  /usr/local/bin/bootstrap-language-binaries --from-remote --only python
fi
exec /usr/local/bin/ballast-python "$@"
