# Dart And Flutter Linting Rules

These rules are intended for Codex (CLI and app).

These rules help configure `analysis_options.yaml`, `flutter_lints`, Dart formatting, `flutter analyze`, and CI linting for Flutter apps.

---
