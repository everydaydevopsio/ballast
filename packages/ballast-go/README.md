# ballast-go

Go CLI package for installing Ballast agent rules and skills.

## Usage

```bash
ballast-go install --target cursor --all
ballast-go install --target codex --agent linting
ballast-go install --target codex --skill speckit-bootstrap
ballast-go install --target cursor --agent linting --patch
```
